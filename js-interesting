## Частичное применение функции в JS

```javascript
   const fn = (x, y) => x * y;
   
   // bind - прикрепление контекста + прием неограниченного числа аргументов
   const test = fn.bind(this, 2);
   console.log(test(2)); //outputs 4

```
